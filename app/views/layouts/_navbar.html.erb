<nav class="navbar navbar-expand-lg navbar-light bgnav fixed-top">
  <a class="navbar-brand icon2" href="<%= root_url %>"><span class="glyphicon glyphicon-home"></span>S</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
          <% if user_signed_in? %>
          <li class="nav-item nav-link"><%= link_to 'Pacientes', pacientes_path %></li>
          <li class="nav-item nav-link"><%= link_to 'New Paciente', new_paciente_path %></li>
    <% end %>
    </ul>
      <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
    <% if user_signed_in? %>
      <% if current_user.admin? %>
        <li class="nav-item nav-link"><%= link_to 'Dashboard', dashboards_index_path %></li>
        <li class="nav-item nav-link"><%= link_to 'Cargos', cargos_path %></li>
        <li class="nav-item nav-link"><%= link_to 'Contactos', contactos_path %></li>
      <% end %>
    <% end %>
  </ul>
    <ul class="navbar-nav navbar-right">
      <% if user_signed_in? %>
        <li class="nav-item nav-link"><a href="#"> Hola <%= current_user.name %>!</a></li>
        <li class="nav-item nav-link"><%= link_to ' Cerrar sesión', destroy_user_session_path, method: :delete %></li>
      <% else %>
        <li class="nav-item nav-link"><%= link_to 'Inicia sesión', new_user_session_path %></li>
        <li class="nav-item nav-link"><%= link_to 'Regístrate', new_user_registration_path %></li>
      <% end %>
    </ul>
  </div>
</nav>
